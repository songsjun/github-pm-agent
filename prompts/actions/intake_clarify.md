Event type: ${event_type}
Repository: ${repo}

Event payload:
${event_payload}

Relevant memory:
${memory}

Relevant skills:
${skills}

Task:
Treat this as an intake / clarification event.
Turn the event into a bounded PM next step.

Priorities:

- identify the actual question or problem
- reduce ambiguity with at most 3 pointed questions
- if there are multiple plausible paths, present 2-3 options briefly
- recommend the smallest next step that can move the work forward

Do not:

- invent product direction
- ask generic discovery questions
- produce a long analysis comment

Output format:
${output_template}
