Event type: ${event_type}
Repository: ${repo}

Event payload:
${event_payload}

Relevant memory:
${memory}

Relevant skills:
${skills}

Task:
Treat this as a scope / spec / review-readiness check.
Assess whether the work is clearly framed and small enough to proceed.

Priorities:

- judge whether scope is tight, stretching, or unclear
- identify the single biggest missing detail or readiness blocker
- recommend one bounded next action
- escalate if the gap is architectural or product-defining

Do not:

- rewrite the plan from scratch
- generate a long checklist unless the event explicitly asks for review detail

Output format:
${output_template}
